<h1>Import AntiMap log file</h1>
<p>File must be a csv</p>

<%= form_for :upload, :url => csv_import_url, :method => :post, :html => { :multipart => true } do |f| -%>
  <%= f.file_field :csv %>
  <%= f.submit "Upload", :disable_with => 'Uploading...' %>
<% end -%>